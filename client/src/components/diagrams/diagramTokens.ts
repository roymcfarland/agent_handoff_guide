/** Shared CSS variable references for SVG diagrams (theme-aware). */
export const D = {
  ink: "var(--ink)",
  inkSoft: "var(--ink-soft)",
  red: "var(--red)",
  paper: "var(--paper)",
  grid: "var(--grid-strong)",
  border: "var(--border)",
  muted: "var(--muted-foreground)",
} as const;
