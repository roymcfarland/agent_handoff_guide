# Current Slice: none — July 2026 roadmap complete

## Status

No slice is in flight. The Advisor-era overhaul (PRs #29–#52) shipped: three-role model, verdict triage, after-merge protocol, ceremony sizing, template upgrade, diagram redraw, title-block footer, CI, hygiene prune, theme toggle, Field Notes + worked example, SEO/share hygiene, prompts-as-files + llms.txt, prerender, sheet index + anchors, byline + FAQ, robot brand mark, and the delight batch.

## Backlog (unscoped — pick one and scope it into this file before building)

- **Express 5 migration** — drops the qs/path-to-regexp security overrides; `app.get("*")` syntax changes in v5.
- **Per-section URLs** — the SEO paging decision; crosses PROJECT.md's "single-page reference" identity, so the spec amendment comes first.
- **Community items** — issue templates for field-note submissions, GitHub releases tagged to the revision table, adopter badge.
- **Viewport maximum-scale removal + a11y audit pass** — small; fold into the next convenient slice.
- **Unify Phase 2 prompts into prompts/** — they were deliberately left out of the generated set (AC scope); emit them if demand appears.

## Convention reminder

Whoever opens the next slice: overwrite this file with the slice scope per the template (Context, Acceptance Criteria with expected test delta, Constraints, Pre-confirmed facts, Files explicitly forbidden, Starting Point), and reset it again at closeout. A stale HANDOFF is an unfinished loop.
